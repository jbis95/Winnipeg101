<% if (pn.pnTotal > 1) { %>
  <div class="pagination">
    <div class="prev">
      <% if (pn.pnNum > pn.pnSize) { %>
        <a href="?<% if (typeof (searchType) !== 'undefined' && searchType) { %>searchType=<%= searchType %>&<% } %>&<% if (typeof (keyword) !== 'undefined' && keyword) { %>keyword=<%= keyword %>&<% } %>page=<%= pn.pnStart - pn.pnSize %>"><%= __('admin_prev') %></a>
      <% } %>
    </div>
    <div class="number">
      <% for (let i = pn.pnStart; i < pn.pnEnd + 1; i++) { %>
        <% if (pn.pnNum === i) { %>
          <a class="selected"><%= i %></a>
        <% } else { %>
          <a href="?<% if (typeof (searchType) !== 'undefined' && searchType) { %>searchType=<%= searchType %>&<% } %>&<% if (typeof (keyword) !== 'undefined' && keyword) { %>keyword=<%= keyword %>&<% } %>page=<%= i %>"><%= i %></a>
        <% } %>
      <% } %>
    </div>
    <div class="next">
      <% if ((pn.pnTotal - pn.pnStart) > pn.pnSize) { %>
        <a href="?<% if (typeof (searchType) !== 'undefined' && searchType) { %>searchType=<%= searchType %>&<% } %>&<% if (typeof (keyword) !== 'undefined' && keyword) { %>keyword=<%= keyword %>&<% } %>page=<%= pn.pnStart + pn.pnSize %>"><%= __('admin_next') %></a>
      <% } %>
    </div>
  </div>
<% } %>